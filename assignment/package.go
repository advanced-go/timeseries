package assignment

const (
	assignmentZone = "assignment/zone" // Should return unique host names for region/zone/sub-zone
	assignmentHost = "assignment/host" // Should return unique instance id's region/zone/sub-zone/host
)
